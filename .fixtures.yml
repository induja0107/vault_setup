fixtures:
  repositories:
    stdlib: "https://github.com/puppetlabs/puppetlabs-stdlib.git"
    epel:
        repo: "https://github.com/stahnma/puppet-module-epel.git"
        ref: "v1.2.2"
  symlinks:
    vault_setup: "#{source_dir}"
